<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
                                 http://www.springframework.org/schema/beans/spring-beans.xsd
                                 http://www.springframework.org/schema/security
                                 http://www.springframework.org/schema/security/spring-security.xsd">
        
        <http auto-config="true" use-expressions="true">
		    <intercept-url pattern="/user/**" access="hasAnyRole('user','admin')"/>
		    <intercept-url pattern="/admin/**" access="hasRole('admin')"/>
		    <intercept-url pattern="/**" access="permitAll"/>
	        <!-- 권한 설정 -->    
		    <form-login
		        username-parameter="UserID"
		        password-parameter="Password"
		        login-page="/login"
		        default-target-url="/loginChk"
		        authentication-failure-url="/login?error=true"
		    />
        </http>
        
        <jdbc-user-service data-source-ref="dataSource" id="userService"
		    users-by-username-query="SELECT UserID, Password, 1 as enabled FROM Users WHERE UserID=?"
		    authorities-by-username-query="SELECT UserID, UserType as authorities FROM Users WHERE UserID=?"
		/>
        
        <authentication-manager>
            <authentication-provider user-service-ref="userService">
            </authentication-provider>
        </authentication-manager>
    
</beans:beans>
