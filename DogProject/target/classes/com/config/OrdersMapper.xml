<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="OrdersMapper">      

<select id="OrderSelect" parameterType="map" resultType="Orders">
	SELECT * FROM ORDERS WHERE UserID=#{UserID} and ProductID=#{ProductID}
</select>

<update id="orderFlagUpdate" parameterType="int">
	UPDATE ORDERS SET ReviewFlag='false' where OrderID=#{OrderID}
</update>
</mapper>
